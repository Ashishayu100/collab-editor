node_modules/
dist/
build/
.env
.env.production
.env.production.local
!.env.example
!.env.production.example
*.log
.DS_Store
coverage/

# AWS deployment runtime artifacts — created at the repo root on the EC2 host (where
# /opt/collab-editor is the git clone itself) by deployment/aws/scripts/*.sh. Never belong in
# the repo.
/logs/
/backups/

# k6 run output — the scripts are committed, the results of any given run are not.
loadtest/results/*
!loadtest/results/.gitkeep

# Prisma
server/prisma/migrations/**/migration.sql.bak

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# OS
Thumbs.db
