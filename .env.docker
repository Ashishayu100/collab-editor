# Defaults for the production docker-compose.yml stack (loaded via
# `docker compose --env-file .env.docker up -d`, see Makefile/package.json's `docker:up`).
# This file is committed as a working starting point — change the secrets below before
# deploying anywhere reachable from outside your own machine.

POSTGRES_USER=collab
POSTGRES_PASSWORD=collab_secret_2024
POSTGRES_DB=collab_editor
POSTGRES_PORT=5432
REDIS_PORT=6379

JWT_ACCESS_SECRET=docker-access-secret-change-in-real-production
JWT_REFRESH_SECRET=docker-refresh-secret-change-in-real-production
CLIENT_URL=http://localhost
ADMIN_EMAILS=

CLIENT_PORT=80
